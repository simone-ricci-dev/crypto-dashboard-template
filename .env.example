# This public demo requires NO environment variables.
# All data comes from public APIs (Binance, Alternative.me, mempool.space, blockchain.info).
#
# This file is only a placeholder for anyone who wants to extend the dashboard
# with their own optional integrations. Never commit real secrets — .env* is gitignored.
